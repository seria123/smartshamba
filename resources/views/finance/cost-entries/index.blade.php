@extends('layouts.app')
@section('content')
    <header class="content-header"><div><p class="eyebrow">Finance / Costing</p><h1>Cost entries</h1></div><a class="button" href="{{ route('finance.cost-entries.create') }}">New cost entry</a></header>
    <form class="filter-bar" method="GET"><select name="status"><option value="">Any status</option>@foreach(['draft','confirmed','void'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><select name="source_module"><option value="">Any source</option>@foreach(['manual','labour','task','inventory','crop','livestock','irrigation','asset','maintenance','other'] as $source)<option value="{{ $source }}" @selected(request('source_module')===$source)>{{ $source }}</option>@endforeach</select><select name="cost_category_id"><option value="">Any category</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('cost_category_id')==$category->id)>{{ $category->name }}</option>@endforeach</select><button class="button secondary" type="submit">Filter</button></form>
    <table><thead><tr><th>Date</th><th>No.</th><th>Title</th><th>Category</th><th>Centre</th><th>Source</th><th>Status</th><th>Amount</th><th>Actions</th></tr></thead><tbody>@foreach($entries as $entry)<tr><td>{{ $entry->entry_date?->toDateString() }}</td><td>{{ $entry->entry_no }}</td><td>{{ $entry->title }}</td><td>{{ $entry->category?->name }}</td><td>{{ $entry->centre?->name }}</td><td>{{ $entry->source_module }}</td><td>{{ $entry->status }}</td><td>{{ $entry->currency }} {{ number_format($entry->amount, 2) }}</td><td><a href="{{ route('finance.cost-entries.show',$entry) }}">View</a></td></tr>@endforeach</tbody></table>{{ $entries->links() }}
@endsection
