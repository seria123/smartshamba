@extends('layouts.app')
@section('content')
    <header class="content-header"><div><p class="eyebrow">{{ $schedule->schedule_number }}</p><h1>{{ $schedule->zone?->name }} schedule</h1></div><div class="actions"><a class="button secondary" href="{{ route('irrigation.schedules.index') }}">Back</a><a class="button" href="{{ route('irrigation.schedules.edit',$schedule) }}">Edit</a><form method="POST" action="{{ route('irrigation.schedules.complete',$schedule) }}">@csrf<button>Complete</button></form><form method="POST" action="{{ route('irrigation.schedules.cancel',$schedule) }}">@csrf<button>Cancel</button></form></div></header>
    <dl class="detail-list"><div><dt>Date</dt><dd>{{ $schedule->scheduled_date?->format('Y-m-d') }}</dd></div><div><dt>Zone</dt><dd>{{ $schedule->zone?->name }}</dd></div><div><dt>Field</dt><dd>{{ $schedule->field?->name ?? '-' }}</dd></div><div><dt>Crop cycle</dt><dd>{{ $schedule->cropCycle?->name ?? '-' }}</dd></div><div><dt>Priority</dt><dd>{{ ucfirst($schedule->priority) }}</dd></div><div><dt>Status</dt><dd>{{ ucfirst(str_replace('_',' ',$schedule->status)) }}</dd></div><div><dt>Events</dt><dd>{{ $schedule->events->count() }}</dd></div><div><dt>Instructions</dt><dd>{{ $schedule->instructions ?? '-' }}</dd></div></dl>
@endsection
